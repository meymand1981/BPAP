{
  "name": "bpap",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prisma": "^7.8.0",
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0"
  }
}