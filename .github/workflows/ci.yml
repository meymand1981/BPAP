name: BPAP CI

on:
  push:
    branches:
      - develop
      - main

  pull_request:
    branches:
      - develop
      - main

jobs:
  validation:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Repository Validation

        run: |
          echo "BPAP Repository initialized successfully."